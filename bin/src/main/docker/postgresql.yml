version: '2'
services:
  riportal-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/riportal/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=riportal
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
